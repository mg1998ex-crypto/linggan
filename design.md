# 灵感 APP 设计文档

## 设计理念

**极简艺术 (Minimalist Art)**
- 大量留白、精致字体排版、克制的色彩(黑白灰或低饱和度色系)
- 无多余弹窗和按钮,界面干净纯粹
- 让用户感受到这是一个"专属的创意冥想室",而非普通的记事本

## 屏幕列表

### 1. 主屏幕 (Home Screen)
**核心功能**: 卷轴滚动动画 + 灵感记录
**主要内容**:
- "开始"按钮(居中,极简设计)
- 三列词语卷轴(老虎机滚动效果)
- 文字输入框(卷轴停止后显示)
- "保存"按钮(输入内容后显示)

**交互流程**:
1. 用户点击"开始"按钮
2. 三列词语快速滚动,依次停下(仪式感动画)
3. 下方出现纯文字输入框
4. 用户输入灵感点子(实时自动保存草稿)
5. 点击"保存"按钮完成存档

### 2. 灵感列表屏幕 (Archive Screen)
**核心功能**: 展示所有已保存的灵感
**主要内容**:
- 灵感卡片列表(每条显示原词、点子摘要、时间戳)
- 点击卡片查看完整灵感内容

**交互流程**:
1. 用户从底部 Tab 切换到列表页
2. 滚动浏览所有已保存的灵感
3. 点击卡片查看详情

## 色彩方案

**极简黑白灰色系**:
- 背景色: 纯白 (#FFFFFF) / 深灰 (#1A1A1A)
- 主文字: 深灰 (#2C2C2C) / 浅灰 (#E5E5E5)
- 次要文字: 中灰 (#8A8A8A)
- 强调色: 极低饱和度蓝灰 (#5A6C7D) - 用于"开始"和"保存"按钮
- 边框/分隔线: 极淡灰 (#F0F0F0) / 深灰 (#2A2A2A)

## 关键用户流程

### 流程 1: 生成灵感
1. 打开 APP → 主屏幕
2. 点击"开始"按钮 → 卷轴滚动动画
3. 三个词依次停下 → 输入框出现
4. 输入灵感点子(自动保存草稿)
5. 点击"保存" → 存档成功 → 回到初始状态

### 流程 2: 查看历史灵感
1. 切换到"灵感列表" Tab
2. 滚动浏览列表
3. 点击卡片 → 查看完整内容

## 排版与布局

### 主屏幕布局(竖屏 9:16)
```
┌─────────────────────┐
│                     │  ← 顶部留白(20%)
│      [开始]         │  ← 按钮居中
│                     │
│   ┌───┬───┬───┐    │  ← 三列卷轴
│   │词1│词2│词3│    │
│   └───┴───┴───┘    │
│                     │
│  ┌───────────────┐  │  ← 输入框(卷轴停止后显示)
│  │               │  │
│  │  输入灵感...  │  │
│  │               │  │
│  └───────────────┘  │
│                     │
│      [保存]         │  ← 保存按钮
│                     │
└─────────────────────┘
```

### 灵感列表布局
```
┌─────────────────────┐
│  灵感列表           │  ← 标题
├─────────────────────┤
│ ┌─────────────────┐ │
│ │ 词1 · 词2 · 词3 │ │  ← 灵感卡片
│ │ 点子摘要...     │ │
│ │ 2026-02-20      │ │
│ └─────────────────┘ │
│ ┌─────────────────┐ │
│ │ 词1 · 词2 · 词3 │ │
│ │ 点子摘要...     │ │
│ │ 2026-02-19      │ │
│ └─────────────────┘ │
│                     │
└─────────────────────┘
```

## 字体排版

- 标题/按钮: 16-18pt, 中等字重(500-600)
- 卷轴词语: 24-28pt, 粗体(700), 字间距加大
- 正文输入: 16pt, 常规字重(400), 行高 1.5
- 时间戳/辅助信息: 12-14pt, 细字重(300-400)

## 动画设计

### 卷轴滚动动画
- 三列同时开始滚动(快速)
- 依次停止: 左列 → 中列 → 右列(间隔 200-300ms)
- 使用缓动函数: ease-out
- 总时长: 约 2-3 秒

### 输入框出现动画
- 淡入 + 轻微上移
- 时长: 300ms
- 缓动: ease-in-out

## 技术实现要点

1. **词库**: 本地 JSON 文件,2000-3000 个中文常见具象名词
2. **同源词过滤**: 语义相似度检测算法
3. **草稿自动保存**: 实时存储到 AsyncStorage
4. **数据存储**: SQLite 本地数据库(Drizzle ORM)
5. **动画**: React Native Reanimated 4.x
